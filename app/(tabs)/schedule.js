import React from 'react';
import { View, Text } from 'react-native';

const Schedule = () => {
    return (
        <View style={{
            flex: 1,
            alignItems: "center",
            justifyContent: "center",
            backgroundColor: "#fff"
        }}>
            <Text className='text-3xl'>Schedule</Text>
        </View>
    );
};

export default Schedule;
